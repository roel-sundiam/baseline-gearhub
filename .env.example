MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/pvtennis?retryWrites=true&w=majority
JWT_SECRET=your-very-secret-key-change-this
VAPID_PUBLIC_KEY=<generate with: node -e "const wp=require('web-push');const k=wp.generateVAPIDKeys();console.log(k)">
VAPID_PRIVATE_KEY=<from above>
VAPID_EMAIL=mailto:admin@yourdomain.com
